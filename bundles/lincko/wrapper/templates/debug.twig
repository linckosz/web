{#
	How to use:
	$abc = 'A variable'
	$app->render('/bundles/lincko/wrapper/templates/debug.twig', array(
		'data' => $abc,
	));

	Then open the link (change the domain name according to dev(.net)/stage(.co)/production(.com) server)
	https://lincko.co/debug/twig
#}
<!doctype html>
<html>
<head>

<script src="{{ _filelatest('/scripts/libs/jquery-1.12.0.min.js') }}" type="text/javascript"></script>

<script src="{{ _filelatest('/scripts/libs/barcode/w69b.qrcode.min.js') }}" type="text/javascript"></script>
<script>

$(function(){
	w69b.qr.decoding.setWorkerUrl("{{ _filelatest('/scripts/libs/barcode/w69b.qrcode.decodeworker.min.js') }}");

	var scanner = new w69b.qr.ui.ContinuousScanner();
      scanner.setDecodedCallback(function(result) {
        alert('decoded: ' + result);
      });
      scanner.render(document.getElementById('reader'));
});

</script>

<style type="text/css">
	body {
		margin: 0;
		padding: 0;
	}
</style>

</head>
<body>
	QR code scanner
	<div id="reader" style="width:300px;height:250px"></div>
</div>
</body>
</html>
