<script>

	//Because this is loaded at very first
	Lincko.storage = {
		COMID: null,
		last_visit: null,
		data: {},
		data_recent: {},
	};

	Lincko.storage.getSha = function(){
		return "{{ sha }}";{# unuse at this moment, but it can be used as prefix or encryption value for AmpliJS storage #}
	}

	Lincko.storage.getUID = function(){
		return "{{ uid }}";
	}

	Lincko.storage.getCOM = function(){
		var url = {% if company is empty %}""{% else %}"{{ company }}"{% endif %};
		return url.toLowerCase();
	}

</script>